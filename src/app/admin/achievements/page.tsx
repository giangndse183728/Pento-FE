import AchievementsManager from '@/features/achievements/components/AchievementsManager';

export const metadata = {
    title: 'Admin - Achievements',
};

export default function AchievementsPage() {
    return <AchievementsManager />;
}
