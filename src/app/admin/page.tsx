'use client';

import Dashboard from '@/features/admin/components/AdminDashboard';

export default function AdminPage() {
    return (
        <main>
            <Dashboard />
        </main>
    );
}